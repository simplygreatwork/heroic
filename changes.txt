@@ -1,9 +1,8 @@
 
 <div>
-       <div id="div_row" class="row"><a id="a_row" class="row" href="#">Item</a></div>
+       <div id="div" class="row"><a id="a" class="row" href="#">Item</a></div>
        <script type="module">
                import {_} from './item.js'
                _()
        </script>
-       
-</div>
+</div>
\ No newline at end of file
diff --git a/examples/list/item.js b/examples/list/item.js
index 4c40640..54f5b1a 100644
--- a/examples/list/item.js
+++ b/examples/list/item.js
@@ -6,7 +6,7 @@ export function _() {
        Component.ready(({ component, data, $, elements }) => {
                
                const { item, link, bus } = data
-               const div = $('div.row'), a = $('a.row')
+               const { div, a } = elements
                
                Object.assign(a, { href: link, innerText: item.title })
                div.onmousedown = () => window.location.hash = link
diff --git a/examples/list/items.html b/examples/list/items.html
index 9a79393..58cad88 100644
--- a/examples/list/items.html
+++ b/examples/list/items.html
@@ -53,6 +53,8 @@
                        const link = `#/list/${item.id}`
                        const row = template.clone({ item, link, bus })
                        cache[item.id] = { item, row }
+                       row.element.style.display = 'block'
+                       template.element.style.display = 'none'
                        return row
                }
                
diff --git a/examples/stress/item.html b/examples/stress/item.html
index 41e2214..9dc766a 100644
--- a/examples/stress/item.html
+++ b/examples/stress/item.html
@@ -1,6 +1,6 @@
 
 <div>
-       <div class="row"><a class="row" href="#">Item</a></div>
+       <div id="div" class="row"><a id="a" class="row" href="#">Item</a></div>
        <script type="module">
                import {_} from './item.js'
                _()
diff --git a/examples/stress/item.js b/examples/stress/item.js
index f6c5ac7..2f1f4d2 100644
--- a/examples/stress/item.js
+++ b/examples/stress/item.js
@@ -3,10 +3,10 @@ import { Component } from 'component'
 
 export function _() {
        
-       Component.ready(function({ component, data, $ }) {
+       Component.ready(function({ component, data, $, elements }) {
                
                const { item, link, bus } = data
-               const div = $('div.row'), a = $('a.row')
+               const { div, a } = elements
                
                Object.assign(a, { href: link, innerText: item.title })
                div.onmousedown = () => window.location.hash = link
diff --git a/examples/tasks/detail.html b/examples/tasks/detail.html
index c2a1f77..31c8983 100644
--- a/examples/tasks/detail.html
+++ b/examples/tasks/detail.html
@@ -30,6 +30,7 @@
                                bus.emit('title-changed')
                        }
                        done.onclick = () => {
+                               console.log(`complete`)
                                item.done = true
                                bus.emit(`item-changed:${item.id}`, { item })
                        }
diff --git a/examples/tasks/task.js b/examples/tasks/task.js
index 85e2927..d1c147d 100644
--- a/examples/tasks/task.js
+++ b/examples/tasks/task.js
@@ -6,11 +6,15 @@ export function _() {
        Component.ready(function({ component, data, $, elements }) {
                
                let { item, link, bus } = data
-               const div = $('div.row'), a = $('a.row')
+               const { div, a } = elements
                
                Object.assign(a, { href: link, innerText: item.title })
                div.onmousedown = () => window.location.hash = link
                bus.on(`item-changed:${item.id}`, ({ item }) => {
+                       
+                       console.log(`listen`)
+                       console.log(`item.done:  ${item.done}`)
+                       console.log(`item.closed:  ${item.closed}`)
                        a.innerText = item.title
                        item.done ? a.classList.add('done') : a.classList.remove('done')
                        item.closed ? component.remove() : null
diff --git a/modules/component/source/component.js b/modules/component/source/component.js
index 5a17755..222cae5 100644
--- a/modules/component/source/component.js
+++ b/modules/component/source/component.js
@@ -75,7 +75,7 @@ export class Component {
                        base: this.base,
                        template: element.hasAttribute('data-template') ? true : false
                })
-               if (child.template) element.style.display = 'none'
+               // if (child.template) element.style.display = 'none'
                this.children.unshift(child)
                child.on('ready', () => this.scan_child(elements))
                child.load()
@@ -138,11 +138,13 @@ export class Component {
                        options: this.options,
                        path: this.path,
                        parent: this.parent,
-                       fn: this.fn
+                       fn: this.fn,
+                       elements: this.elements,
+                       template: false
                })
                this.parent.children.push(component)
                this.element.before(component.element)
-               component.element.style.display = 'block'
+               // component.element.style.display = 'block'
                component.invoke()
                return component
        }